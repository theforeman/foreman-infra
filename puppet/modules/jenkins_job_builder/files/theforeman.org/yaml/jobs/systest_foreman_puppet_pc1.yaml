- job:
    name: systest_foreman_puppet_pc1
    project-type: matrix
    logrotate:
      daysToKeep: -1
      numToKeep: 3
    triggers:
      - timed: 'H 10 * * 6'
    builders:
      - trigger-builds:
        - project: systest_foreman
          predefined-parameters: "os=${os}\npl_puppet=4"
          block: true
    axes:
      - axis:
          type: user-defined
          name: os
          values:
          - centos7
          # - debian9  # not available yet
          # - ubuntu1604  # https://tickets.puppetlabs.com/browse/SERVER-17
    publishers:
      - ircbot_freenode
