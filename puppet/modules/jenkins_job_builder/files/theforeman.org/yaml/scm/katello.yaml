- scm:
    name: katello
    scm:
      - git:
          url: git://github.com/Katello/katello.git
          wipe-workspace: true
          basedir: 'plugin'
          branches:
            - '${branch}'
