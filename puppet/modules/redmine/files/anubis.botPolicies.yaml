---
bots:
  - import: (data)/meta/default-config.yaml
  - name: does-not-need-to-have-accept
    expression: '!("Accept" in headers)'
    action: WEIGH
    weight:
      adjust: -5

openGraph:
  enabled: true
